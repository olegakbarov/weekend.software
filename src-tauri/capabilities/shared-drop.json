{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "shared-drop",
  "description": "Capability for the shared-drop utility window.",
  "local": true,
  "windows": ["shared-drop"],
  "permissions": [
    "allow-app-commands",
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
